# Extension icons

Need `icon16.png`, `icon48.png`, `icon128.png` here (referenced by `manifest.json`).
Placeholder: any square PNG works for local `chrome://extensions` testing — swap for a
real BiteCheck mark before the demo recording.
