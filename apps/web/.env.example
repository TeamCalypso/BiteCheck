# Copy to .env for local dev. Not a secret - this is a public API Gateway URL - but kept
# out of git anyway since it'll change if the stack is ever torn down and redeployed.
# The live Amplify site sets this same value in its own build environment variables
# (Amplify Console -> App settings -> Environment variables), not from this file.
VITE_API_BASE_URL=https://5607b13rz9.execute-api.us-east-1.amazonaws.com/prod
